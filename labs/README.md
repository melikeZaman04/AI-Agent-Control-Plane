# Architect OS Labs

Labs are planned controlled scenarios for LEARN and RESEARCH modes. A future lab may define a scenario, repository fixture, hidden failure, expected behavior, success criteria, and benchmark configuration.

Example failures include hidden dependencies, race conditions, deadlocks, stale context, hallucinated APIs, and non-idempotent retries.

The labs runtime is not part of M0 and is not implemented here.
