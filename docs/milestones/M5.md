# M5 — Context Economy acceptance

Status: COMPLETE, 2026-09-15, under A3.

LocalContext supplies bootstrap context, Git HEAD tracking, incremental
hash/symbol indexing, a deterministic repo map, targeted path/symbol search,
exact provenance-linked source reads, context metrics and a KnowledgeProvider
protocol. Optional NotebookLM is deferred. No new runtime dependency or core
schema was introduced; an atomically replaced local metadata-only JSON cache
lives under `.architect`. Current tracked working-tree content remains truth.

Six real-Git tests cover hash reuse, dirty edits, deletion, HEAD advancement,
newly staged/untracked files, syntax errors, binary/symlink exclusion, exact reads,
source-text budgets, metadata-only caching, CLI execution and stale membership.

Risk-justified independent review caught an index-membership gap: a file removed
from Git tracking could still be read from cached metadata. Exact current regular
stage-0 membership is now checked on every read. A regression test reproduces the
problem and verifies rejection. Follow-up independent verdict: M5 focused review
PASS, with 6 tests passing in 0.20s. No remaining reviewed findings.

Validation:

- Parent baseline: M4 full suite 220 passed in 1.72s.
- Targeted final: 6 passed in 0.22s.
- Full `.venv/bin/python -m pytest -q` after repair: **226 passed in 1.92s**.
- Diff/status, whitespace, boundaries and scope reviewed before local checkpoint.

Limits: refresh hashes bytes to protect correctness; only parsing is reused.
Only regular tracked UTF-8 files up to 1 MiB qualify; skipped/omitted sources are
reported. Bootstrap uses source-character budgets and a labeled chars/4 token
estimate, not native usage. Cache is local metadata, not a security sandbox or
source archive. No prompts, external accounts, model calls or knowledge service
connections are introduced. The provider protocol keeps future sources optional.

M5 is complete. Create a local checkpoint, then continue to M6 under A3.
