# M6 — Research / Benchmark

Status: COMPLETE under A3, 2026-09-15.

Implemented explicit validated suites, repeated real subprocess trials from fresh
pinned Git archives, timeout/process-group cleanup, deterministic success
predicates, local receipts and comparisons. Source/context/suite hashes preserve
provenance. Commands and output are absent from receipts. Declared models and
command-reported usage are labeled; missing usage remains unknown. No new schema,
dependency, provider adapter, scheduler or external service.

Validation: full regression **233 passed in 2.44s**. Seven real Git/subprocess/CLI
tests cover repetition isolation, context matching, token metrics, dirty sources,
timeouts, launch/exit failure, validation before effects and comparison identity.
Risk-based independent review also exercised descendant cleanup on parent exit
and timeout. Its case-pooling finding was repaired and regression-tested; repeated
receipt input is rejected to prevent double counting. git diff --check passed.

Limits: POSIX trusted commands retain user permissions/environment; fresh source
copies are not sandboxes. No remote model was benchmarked during development.
Output evaluation is bounded but transient disk spools have no separate quota.
Git source objects and original suite files must be retained for later resolution.
Future sandbox/resource-quota work requires its own product/security contract.
