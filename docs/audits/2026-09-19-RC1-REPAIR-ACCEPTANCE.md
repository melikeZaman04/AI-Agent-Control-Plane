# RC1 repair branch product acceptance — 2026-09-19

**PRODUCT ACCEPTANCE AUDIT: CONDITIONAL PASS**

This campaign audits the implemented source at `cbe034374683b65f995861695ea44b7ae6490849` on `rc1-acceptance-repair`, built from the clean `17f8b61a69a7737cc65b102260dc4ec8b10098e5` baseline. A1–A6 were reproduced, repaired, tested and committed separately. Fresh Claude and Codex observations, the evidence chain, M6/M7 product paths, and independent adversarial probes passed. The external fresh-agent M5 correctness check still lacks authorization to send this repository's documentation to Codex, so that material acceptance evidence remains open. No Roadmap v2 scope or product feature was added.

## Baseline, preservation and commits

**FACT —** The original `main` checkout was dirty at `17f8b61`; its exact initial porcelain status and pre-repair FAIL findings are preserved in the [HEAD RC1 audit](2026-09-19-RC1-HEAD-ACCEPTANCE.md). Before branching, `/tmp/architect-rc1-backup-20260919` captured the exact HEAD, porcelain status, binary tracked diff, staged diff, and all untracked files. SHA-256 verification passed. Applying the patch and archive to a fresh clone restored all **37** changed/untracked files byte-for-byte. The original `main` working tree was left untouched.

**FACT —** `git worktree add -b rc1-acceptance-repair ... 17f8b61` created the clean source baseline. The temporary `.venv` symlink was the only untracked entry before changes; `.architect` index data was created only during acceptance and is not a source change. The environment was Linux/POSIX, Python 3.14.7, the existing project virtual environment, local Git/SQLite and explicit 127.0.0.1 access. The virtual environment's editable installation was overridden with `PYTHONPATH=/tmp/architect-rc1-acceptance-repair/src` for every Python test and CLI path. Baseline regression: **239 passed in 3.79s** with loopback access. `git diff --check` passed at audit close.

| Finding | Before repair | After repair | Targeted / full regression | Local commit |
|---|---|---|---|---|
| A1 evidence validation | 12 new counterexamples failed | All 12 passed; legacy rows remain distinct | 94 / 251 passed | `394c525` |
| A2 day boundaries | 3 of 4 new cases failed | Eventless starts and one-snapshot reads passed | 74 / 255 passed | `d4aa13a` |
| A3 retry oracle | Two wrong repairs passed and revision was absent | Both wrong repairs rejected; old checker remains pinned | 25 / 258 passed | `bc9851a` |
| A4 bootstrap delivery | Missing `{context_file}` was accepted | Suite rejected before any command; real bundle bytes verified | 30 / 262 passed | `9a3d8d2` |
| A5 Git provenance | Tree object accepted as `commit_id` | Tree/blob objects rejected | 32 / 263 passed | `8fa59f2` |
| A6 documentation | CLI/Storage and deferred statuses were stale | Nine targeted semantic/link checks passed | 9 document checks / 263 passed | `cbe0343` |

**MEASUREMENT —** The final full suite after A6 was **263 passed in 4.47s**; no tests were excluded, and the OTLP loopback tests ran. Each checkpoint was preceded by a targeted run, full regression, staged `git diff --check`, and a scoped commit. The 11 relevant implementation/test files on this branch match the previously uncommitted repair package byte-for-byte. Documentation was adjusted for this branch's actual status and links.

## Fresh provider acceptance and provenance

**FACT — Claude.** The real Claude Code 2.1.259 CLI ran in disposable Git project `/tmp/architect-rc1-repaired-claude`, using the repository's opt-in hook template and the repaired branch's `architect` executable. Architect run `b3d33946-2a43-4d31-819d-3a5baa6605e4` bound native session `d06c0075-d0ff-4525-b757-e7f4dbc02ee8`. Claude read README and reported `1 passed in 0.00s`. Hook observations reached Flight Recorder as `file_read` evidence `753fb457-b4d8-4d34-9ebb-360cc4d2f6e9` and `tool_execution` evidence `d76729a0-9143-4054-8754-71a204477aac`. Claude chose a piped pytest command; its provider response contains the passing result, while normalized tool status stays unknown and no `test_execution` success is inferred. Both observations have `provider=claude`, `fidelity=NATIVE`, `synthetic=false`, receipt-time provenance, and the same native session ID. The Architect run remains `RUNNING`, with no inferred end.

**FACT — Codex.** The real Codex 0.154.0-alpha.6.1 CLI ran in disposable Git project `/tmp/architect-rc1-repaired-codex` with invocation-only binary OTLP export to the foreground listener on `127.0.0.1:14329`. Architect run `ff40796d-cf8f-41b3-9bb8-e29b6d7c9870` bound native session `01a0b9cf-9526-7583-b152-2577a654bedf`. Codex read README and ran the smoke test (`1 passed in 0.00s`). After flush, the receiver reported **6 recorded, 22 ignored, 0 rejected**: two approval observations and four generic tool results. The four tool results are not promoted to test results because native telemetry omits commands. All six carry the bound session, `provider=codex`, NATIVE fidelity and `synthetic=false`. The run remains `RUNNING`.

**FACT — complete chain.** Native Claude hooks / Codex OTLP → provider observer → `ArchitectEvent` → `FlightRecorder` SQLite rows → `ProjectChronicle` episodes/receipts → `SessionIntelligence` reports were exercised. Source row IDs, recorder timeline IDs and Chronicle episode IDs agree in deterministic chronology: **2/2 Claude** and **6/6 Codex** evidence IDs resolve exactly. CLI `session status`, `changes`, `day`, `resume`, `explain` and `chronicle`/receipts/failures/evidence all returned valid JSON for both projects. A missing evidence ID returned exit 1, empty stdout and nonempty stderr. `explain` left cause unknown and recommendations empty. Fixture Git commits preceded the provider runs; Git history was listed independently and never attributed to a run. A separate PASSIVE probe remained PASSIVE. Native payload names, sessions, Claude tool inputs/responses and allowlisted Codex OTel fields remain in event metadata; recorder/Chronicle domain fields and imports remain provider independent. Actual provider process launches and native transport establish this smoke's NATIVE origin; `synthetic=false` alone would not.

## M5 Context Economy

**MEASUREMENT —** A fresh local process followed `docs/EXECUTION_PROTOCOL.md` progressive discovery. The generous control read 13 canonical entry/architecture/roadmap/glossary/ADR documents: **66,500 source characters, 66,823 UTF-8 bytes**, 13 explicit reads, 0.306 ms in one observation. The implemented M5 bootstrap plus exact targeted ROADMAP, ARCHITECTURE and EXECUTION_PROTOCOL ranges delivered **22,467 source characters, 22,674 UTF-8 bytes**, 7 unique files / 9 ranges, 97.021 ms with a cold index. Bootstrap itself delivered 11,058 source characters and labeled its 2,765-token `chars/4` estimate; it indexed 77 tracked files. It selected AGENTS, STATUS, PRODUCT and an unnecessary session test, while omitting ROADMAP and ARCHITECTURE under the 12,000-character budget. Targeted reads supplied those missing contract sections. The index hashes all tracked sources, so nine visible source reads do not mean nine filesystem reads. These are source-payload measurements, not comparable model token or task-performance measurements; no reduction percentage is claimed.

**LIMITATION —** Automatic approval review previously rejected an authenticated fresh Codex session that would transmit repository documentation to the external provider. The stated reason was lack of specific authorization for that repository payload. This audit did not bypass the rejection. A fresh agent's five answers, actual model tokens, file/tool trace, dependency misses and repair loops remain unmeasured. The deterministic local probe establishes available context and provenance, but cannot establish agent correctness.

## M6 / M7 product paths

**FACT — M6.** In a disposable clean clone of audited source `cbe0343`, the real `architect benchmark run` path rejected a bootstrap suite without `{context_file}` before its preceding side-effect command ran; no receipt directory or marker was created. Two executions of a valid suite produced receipts `c2476230-3558-40dd-80aa-2784a57de8a8` and `3e97067f-888f-496c-b127-ce9932f6a383`, both with suite SHA-256 `1a7bdc21d7eced6b4519e23da365647e7d7294f75b93be735793d8c7adf1b3a7` and source archive SHA-256 `5a6fda5058917f2a4c9285be52c3ab5bd738137e6f784e8008b8344561da7027`. The bootstrap fixture opened the actual JSON bundle and printed its SHA-256; receipt output hashes matched the expected hash derived from `context_sha256`. `benchmark compare` kept strategy/case groups separate: bootstrap 4/4, none 4/4, intentionally wrong output 0/2. The oracle is the documented pinned Git source, M5 context delivery, command exit and output predicate; no Flight Recorder run link or model-quality claim is invented. No model token use was supplied or inferred.

**FACT — M7.** The real `architect lab list/start/check/progress` path created retry revision-2 session `f9adcd7e441940aba5fceb0f20db014e` in a disposable project. Attempts 1–3 (faulty fixture and two plausible wrong repairs) failed with exit 1 and retained failure byte counts/hashes. A correct per-state request-ID implementation passed attempts 4 and 5. All attempts kept the same pinned checker hash, distinct learner file hashes when content changed, and sequential receipt numbers. Progress marked an intervening edit stale, then matched the final checked source; `mastery` stayed unknown. The checker and evaluated source remain inspectable. Receipts do not retain raw stderr text, consistent with the current contract.

## Reliability and adversarial probes

**MEASUREMENT —** A separate disposable Git/SQLite/loopback script returned true for each of these checks: duplicate event rejection; equal-timestamp insertion ordering and repeatability; explicit FAILED run end and three source-linked failures; failed test inclusion in a receipt; PASSIVE fidelity retention; unknown/wrong-run evidence rejection; provider-session conflict rejection and exact binding; legacy payload non-promotion; damaged normalized row rejection by Recorder, Chronicle and CLI without source mutation or partial stdout; eventless day boundary inclusion; stale content and removed Git membership rejection; tree-object provenance rejection; atomic rollback and recovery after injected event-insert failure; local OTLP listener recovery after malformed HTTP, sensitive-field allowlisting, native-session binding, and conflicting-session partial rejection. No defect was found by these independent probes. Test fixtures and probe outputs were held under `/tmp/architect-rc1-repair-*`; the permanent A1–A5 counterexamples are in [tests/test_acceptance.py](../../tests/test_acceptance.py).

## GitHub and remaining limits

**FACT —** `origin` is `https://github.com/melikeZaman04/AI-Agent-Control-Plane.git`; public GitHub metadata reports default branch `main`, no repository rulesets and zero Actions workflows. The public `main` branch is reported unprotected. `git ls-remote` showed remote main `0773334c3a0575bcde35b3aa357465a5b235063f` and no existing `rc1-acceptance-repair` branch. A no-write `git push --dry-run` for the new development branch succeeded. The GitHub CLI reports its stored token invalid, so it cannot currently create or inspect a PR. Publication is assessed separately against the final audited tree.

**LIMITATION —** Native provider logs are operational, not cryptographic, provenance. Telemetry delivery and retry deduplication are not guaranteed by the current contract. M5's external fresh-agent correctness probe is the only material core-claim acceptance check left without current real-world evidence. M2.4 passive observation, optional NotebookLM, handoff, A4/new roadmap, agent runtime orchestration and stronger execution isolation remain explicitly deferred and were not counted as failures.

**INFERENCE — verdict.** A1–A6 repairs and the current live/CLI paths behaved correctly in the tested conditions; no correctness, fidelity, provenance, recovery or contract violation was demonstrated on the repaired source. The fresh-agent M5 outcome remains unverified after the automatic approval rejection, so unconditional PASS is not supported. **PRODUCT ACCEPTANCE AUDIT: CONDITIONAL PASS** for audited source `cbe0343`.
